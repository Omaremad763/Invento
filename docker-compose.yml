services:
  db:
    image: postgres:16
    container_name: invento-db
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      # استخدمنا المتغيرات هنا برضه عشان لو غيرت اليوزر في الـ .env الـ healthcheck ما يضربش
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - invento-network

  redis:
    image: redis:alpine
    container_name: invento-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
    networks:
      - invento-network

  maildev:
    image: maildev/maildev
    container_name: invento-maildev
    ports:
      - "1080:1080"
      - "1025:1025"
    networks:
      - invento-network

  api:
    build:
      context: .
      dockerfile: Presentation/Dockerfile
    container_name: invento-api
    ports:
      - "8080:8080"
    # هنا بنقول لدوكر يقرأ كل المتغيرات من ملف الـ .env بتاعنا
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      maildev:
        condition: service_started
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      # لاحظ هنا: القيم دي هتعمل Override للقيم اللي في الـ .env
      # عشان جوه دوكر الـ Host هو اسم الـ container (maildev) مش localhost
      - EmailSettings__Host=maildev
      - EmailSettings__Port=1025
      - EmailSettings__Username=""
      - EmailSettings__Password=""
    networks:
      - invento-network

  frontend:
    build:
      context: ./Front
      dockerfile: Dockerfile
    container_name: invento-frontend
    ports:
      - "4200:80"
    depends_on:
      - api
    networks:
      - invento-network

volumes:
  postgres_data:

networks:
  invento-network:
    driver: bridge
